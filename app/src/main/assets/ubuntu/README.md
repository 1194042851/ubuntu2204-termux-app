# Ubuntu rootfs asset

Place the runtime rootfs package here before building an APK that should install Ubuntu offline:

- `rootfs-arm64.tgz`
- `rootfs-arm64.sha256`

These files are intentionally not committed to Git because the rootfs package is very large.
